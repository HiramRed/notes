可以使用：
    $git rm <file>
来删除已经添加入仓库的文件，并放入暂存区
其他参数：
    $rm <file>  
        -- 直接删除文件，不放入暂存区
    $git rm -r  
        -- 删除工作区和暂存区文件，并将这次删除放入暂存区
    $git rm --cache
        -- 将这次删除替换掉暂存区文件，但保留工作区文件
            (保留工作区文件，删除暂存区和版本库文件)
